import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.grupo3.app',
  appName: 'proyecto-grupo3',
  webDir: 'dist/proyecto-grupo3/browser',
  bundledWebRuntime: false,
};

export default config;
